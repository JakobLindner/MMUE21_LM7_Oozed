<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.tuwien.mmue_lm7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MMUELM7">
        <activity android:name=".VideoActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WinActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen" />
        <activity
            android:name=".LivesActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_lives"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen" />
        <activity
            android:name=".GameOver"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen" /> <!-- alwaysRetainTaskState: dont let system kill state of this activity after some time -->
        <activity
            android:name=".GameActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_game"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen" />
        <activity android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.MMUELM7.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>